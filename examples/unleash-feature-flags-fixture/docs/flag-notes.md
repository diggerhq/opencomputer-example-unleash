# Flag notes

`docs-only-cleanup` appears only in documentation. Even if it is old and
enabled in production, the cleanup agent should not open a pull request for it.

This file also mentions `fixture-checkout-v2`, but executable references in
`src/` are the evidence that makes that flag actionable.
